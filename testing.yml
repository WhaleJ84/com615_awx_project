---
- hosts: all
  
  pre_tasks:
    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 86400
      changed_when: false
      become: true
      
  tasks:
    - name: Ensure Firefox is installed
      apt:
        name: firefox
        state: latest
      become: true
      
    - name: Ensure SNMP client is set up
      include_role:
        name: whalej84_snmp_client
...
